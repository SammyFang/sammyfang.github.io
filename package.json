{
  "name": "sammyfang-personal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check script.js && node --check data/site-data.js",
    "test": "node --test tests/*.test.mjs"
  }
}
